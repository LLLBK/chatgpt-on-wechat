# Copy this file to ".env" and fill in your private keys.
# Lines that start with "#" are ignored.
# GEMINI_API_KEY is used for both text and image models.
GEMINI_API_KEY=replace_with_your_gemini_api_key
